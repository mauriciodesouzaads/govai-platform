node_modules/
dist/
.turbo/
coverage/
*.log
.DS_Store
.vscode/
.idea/
*.tsbuildinfo
.pnpm-store/

# Local secrets
.env
.env.*
!.env.example

# KMS / cryptographic material — never commit. The GovAI master seed lives ONLY as
# KMS-encrypted ciphertext in a file OUTSIDE this repo (see docs/runbooks/kms-production.md).
*.ciphertext
*.seed
*master-seed*
*.key
*.pem
*.bin
