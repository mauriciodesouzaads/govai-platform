{
  "name": "govai-platform",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=10.33.2"
  },
  "scripts": {
    "build": "pnpm -r --parallel build",
    "lint": "eslint --max-warnings=0 .",
    "typecheck": "pnpm -r --parallel typecheck",
    "test": "vitest run --reporter=default",
    "test:integration": "GOVAI_INTEGRATION=1 vitest run --reporter=default",
    "test:live": "GOVAI_LIVE_TESTS=1 vitest run --config vitest.live.config.ts",
    "test:obs": "vitest run --config vitest.live.config.ts tests/live/observability-collector.test.ts",
    "test:user-e2e": "vitest run --config vitest.live.config.ts tests/live/user-e2e.test.ts",
    "acceptance:ai-console": "tsx tests/acceptance/ai-console/run.ts",
    "acceptance:ai-console:live": "tsx tests/acceptance/ai-console/run.ts live"
  },
  "devDependencies": {
    "@types/node": "24.10.0",
    "@typescript-eslint/eslint-plugin": "8.13.0",
    "@typescript-eslint/parser": "8.13.0",
    "eslint": "9.13.0",
    "prettier": "3.6.2",
    "@vitest/coverage-v8": "4.1.5",
    "tsx": "4.21.0",
    "typescript": "5.9.3",
    "vitest": "4.1.5"
  },
  "pnpm": {
    "overrides": {
      "zod": "4.4.2"
    },
    "peerDependencyRules": {
      "ignoreMissing": []
    },
    "onlyBuiltDependencies": [
      "argon2",
      "re2",
      "esbuild",
      "protobufjs",
      "cpu-features",
      "ssh2"
    ]
  }
}
