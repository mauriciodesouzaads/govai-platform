# Source Spec — Fontes canônicas externas

Este monorepo é uma **derivação** das fontes canônicas que vivem fora dele, em `../docs/` (relativo à raiz do monorepo, ou seja, `~/Projects/GovAI GRC Platform/docs/`):

- `govai_adp_v3.md` — fonte exclusiva de verdade arquitetural.
- `govai_claude_code_prompt_v2.md` — prompt operacional v2.

**Não duplicar essas fontes dentro de `govai-platform/docs/`.** A pasta interna contém apenas docs do projeto (ADRs, contracts, runbooks).

SHAs verificáveis em `baseline-decisions.md` §5.14 do relatório final.
