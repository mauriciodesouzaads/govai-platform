# ICP-Brasil A3/HSM — `planned`

Não implementado no baseline. Não há claim regulatório externo.

Quando promover para `supported`:
- Integrar com HSM A3 ou nuvem ICP-Brasil credenciada.
- `evidence_strength = 'icp_brasil_tsa'` em `audit_events`.
- Capability deve apresentar facet com `evidence_strength = 'icp_brasil_tsa'` e
  `last_live_test_at` recente.
- Nenhum claim "regulatory-grade" pode ser exposto sem assinatura ICP real.
